const constant = require('./dist/constant');
const createResponseFunction = require('./dist/createResponseFunction');
const createResponse = require('./dist/createResponse');

module.exports = {
  constant,
  createResponseFunction,
  createResponse
};
